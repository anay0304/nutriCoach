// Placeholder API route — replace with real logic when adding backend
import { NextResponse } from "next/server"

export async function GET() {
  return NextResponse.json({
    message: "Placeholder — backend logic to be added",
    status: "ok",
  })
}

export async function POST() {
  return NextResponse.json({
    message: "Placeholder — backend logic to be added",
    status: "ok",
  })
}
